<ul id='sortable_list'>
  <%= render :partial => 'beer_brand', :collection => BeerBrand.find(:all, :order => "main_category ASC, sub_category ASC, position ASC") %>
</ul>
<%= render :partial => "/shared/admin/sortable_list", :locals => {:continue_reordering => (defined?(continue_reordering) ? continue_reordering : true)} %>
